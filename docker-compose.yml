# this is for development
version: '2'
services:
    web:
        build: .
        ports:
            - "8888:80"
        env_file:
          - docker-compose-dev-env
        #volumes:
        #    - .:/code
